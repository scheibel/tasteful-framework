#pragma once

#include <views/BlogView>
#include <models/BlogPost>

class BlogPostShow : public BlogView {
	public:
		BlogPostShow(BlogPost* blogPost, unsigned id);
		
		QDomNode getBlogPost(QDomNode node);
	private:
		BlogPost* blogPost;
		unsigned id;
};
