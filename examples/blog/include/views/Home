#pragma once

#include <views/BlogView>
#include <models/BlogPost>
#include <QList>

class Home : public BlogView {
	public:
		Home(QList<BlogPost*> blogPosts);
		
		void recentPosts(DomNode& node);
		void numberOfRecentPosts(DomNode& node);
	private:
		QList<BlogPost*> blogPosts;
};
