#pragma once

#include <views/BlogView>
#include <QString>

class Login : public BlogView {
	public:
		Login(QString email = "", bool loginAttempt = false);
		
		void loginForm(DomNode& node);
		void errorMessage(DomNode& node);
		void emailValue(DomNode& node);
	private:
		bool loginAttempt;
		QString email;
};
