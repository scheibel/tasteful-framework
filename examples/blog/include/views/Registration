#pragma once

#include <views/BlogView>
#include <QString>

class Registration : public BlogView {
	public:
		Registration(QString email = "", QString error = "");
		
		void registrationForm(DomNode& node);
		void errorMessage(DomNode& node);
		void emailValue(DomNode& node);
	private:
		QString email;
		QString error;
};
