#pragma once

#include <views/BlogView>
#include <models/BlogPost>

class BlogPostEdit : public BlogView {
	public:
		BlogPostEdit(BlogPost* blogPost, unsigned id);
		
		void blogEditForm(DomNode& node);
		void blogPostId(DomNode& node);
		void blogPostTitle(DomNode& node);
		void blogPostText(DomNode& node);
		void saveButtonText(DomNode& node);
	private:
		BlogPost* blogPost;
		unsigned id;
};