#pragma once

#include <views/BlogView>
#include <models/BlogPost>

class BlogPostEdit : public BlogView {
	public:
		BlogPostEdit(BlogPost* blogPost, unsigned id);
		
		QDomNode blogEditForm(QDomNode node);
		QDomNode blogPostId(QDomNode node);
		QDomNode blogPostTitle(QDomNode node);
		QDomNode blogPostText(QDomNode node);
		QDomNode saveButtonText(QDomNode node);
	private:
		BlogPost* blogPost;
		unsigned id;
};