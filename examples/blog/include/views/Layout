#pragma once

#include <TemplateLayout>
#include <session/Session>

class Layout : public TemplateLayout {
	public:
		Layout();
		
		void setSession(Session* newSession);
	protected:
		void stylesheets(DomNode& node);
		void login(DomNode& node);
		void menu(DomNode& node);
	private:
		Session* session;
};
