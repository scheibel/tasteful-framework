#pragma once

#include <views/BlogView>
#include <models/BlogPost>
#include <QHash>

class BlogPostList : public BlogView {
	public:
		BlogPostList(QHash<unsigned, BlogPost*> blogPosts);
		
		void blogPostTable(DomNode& node);
	private:
		QHash<unsigned, BlogPost*> blogPosts;
};
